language: node_js
node_js:
  - 8.9.0

branches:
  only:
    - master
    - develop
    - / ^feature\/.* /

before_script:
  - npm install -g angular-cli

script:
  - ng build --prod

notifications:
  email:
    on_failure: change
    on_success: change
